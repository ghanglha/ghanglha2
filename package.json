{
  "name": "jekyll-tinacms-site",
  "version": "1.0.0",
  "description": "Jekyll + TinaCMS site deployable on Vercel",
  "scripts": {
    "dev": "bundle exec jekyll serve",
    "build": "gem install bundler && rm -rf _site && bundle config set deployment true && bundle install && bundle exec jekyll build && tinacms build"
  },
  "dependencies": {
    "@tinacms/cli": "^1.11.0",
    "tinacms": "^1.5.20"
  },
  "overrides": {
    "final-form": "4.20.8"
  }
}
